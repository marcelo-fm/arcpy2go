// Code generated by arcpy2go; DO NOT EDIT.
package {{.PackageName}}

import (
  "fmt"
  "strings"
)

{{$prefix := .FunctionName}}
// {{.FunctionComment}}
type {{.FunctionName}} struct {
  {{range .Parameters -}}
  // {{.Comment}}
  {{.Name | camelCase}} {{if not .Required}}*{{end}}{{if len .Enums}}{{$prefix}}{{.Name | camelCase}}{{else}}string{{end}}
  {{end}}
  // Comments are not part of the arcpy function signature, but are used to add additional information to the generated code.
  Comments []string
}

func (p {{.FunctionName}}) Name() string {
    return "{{.Command}}"
}

func (p {{.FunctionName}}) Command() string {
  {{- if .IsAssignment }}
  return fmt.Sprintf("%s = %s", p.Name(), p.Args())
  {{- else }}
    return fmt.Sprintf("%s(%s)", p.Name(), p.Args())
  {{- end }}
}

func (p {{.FunctionName}}) CommentsString() string {
  return strings.Join(p.Comments, "\n")
}

func (p {{.FunctionName}}) String() string {
  return p.Command()
}

func (p {{.FunctionName}}) Args() string {
	{{- if .IsAssignment }}
  return p.{{.AssignmentValueParam | camelCase}}
	{{- else }}
  var params []string
  {{range .Parameters -}}
  {{- if not .Required}}if p.{{.Name | camelCase}} != nil { {{end -}}
  params = append(params, fmt.Sprintf("{{.Name}}='%s'", {{if not .Required}}*{{end}}p.{{ .Name | camelCase}}))
  {{- if not .Required}} } {{end}}
  {{end -}}
  return strings.Join(params, ",")
	{{- end }}
}

{{- range .Parameters}}
{{- if len .Enums}}
type {{$prefix}}{{.Name | camelCase}} string
{{$paramName := .Name}}
const (
{{range .Enums -}}
// {{.Comment}}
{{$prefix}}{{$paramName | camelCase}}{{.Name | camelCase}} {{$prefix}}{{$paramName | camelCase}} = "{{.Name}}"
{{end -}}
)
{{- end -}}
{{- end -}}

